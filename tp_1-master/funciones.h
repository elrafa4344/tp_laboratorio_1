/** \brief resta dos numeros enteros
 *
 * \param primer operando ingresado por el usuario
 * \param segundo operando ingresado por el usuario
 * \return el resultado de la resta entre "x" e "y"
 *
 */
int resta (int n1, int n2);
/** \brief suma dos numeros enteros
 *
 * \param n1 int primer operando ingresado por el usuario
 * \param n2 int segundo operando ingresado por el usuario
 * \return int el resultado de la suma entre "x" e "y"
 *
 */
int suma(int n1, int n2);
/** \brief multiplica dos numeros enteros
 *
 * \param  primer operando ingresado por el usuario
 * \param segundo operando ingresado por el usuario
 * \return el resultado de la multiplicacion entre "x" e "y"
 *
 */
int multiplicar (int n1, int n2);
/** \brief divide dos numeros enteros
 *
 * \param primer operando ingresado por el usuario
 * \param segundo operando ingresado por el usuario
 * \return el resultado de la division entre "x" e "y"
 *
 */
float dividir (int n1, int n2);
/** \brief realiza la factorizacion de un entero
 *
 * \return int numero ingresado
 *
 */
int factorial();

